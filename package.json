{
  "name": "cj-codegen",
  "version": "1.0.0",
  "description": "Code generator for cj JIT framework",
  "private": true,
  "scripts": {
    "generate:x86": "node codegen/x86_encoder.js",
    "generate:arm64": "node codegen/arm64_encoder.js",
    "generate": "npm run generate:x86 && npm run generate:arm64"
  },
  "dependencies": {
    "asmdb": "^0.0.3"
  }
}
